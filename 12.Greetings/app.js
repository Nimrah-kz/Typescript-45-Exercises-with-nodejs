"use strict";
let members = [`nimrah`, `ahmed`, `huziafa`, `hafsa`, `wasiq`];
let message = `today is my birthday:`;
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
